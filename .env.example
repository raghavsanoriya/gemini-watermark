# Public browser credentials used only by the optional Google Drive Picker.
# Google OAuth access tokens are requested at runtime and are never written here.
NEXT_PUBLIC_GOOGLE_CLIENT_ID=
NEXT_PUBLIC_GOOGLE_API_KEY=
NEXT_PUBLIC_GOOGLE_APP_ID=
